module simple imports libstring {
	void f() {
		buffer b;
		b = create(5);
		b = "bla";
		b.length;
		buffer a;
		a = b;
		a[0] = 'a';
		int c = 42;
		a[c] = 'b';
		print(a);
		printf("%s %s", b);
		
		FILE f = fopen("a", "r"); // open file
		fread(b, f);          // read file into buffer
	}	
}